Prepare o PR desta feature. NÃO me dê nenhum comando de escrita antes da fase 3.

TAREFA: <cole a spec.md e os N_task.md — critérios de aceite inclusos>
DIFF: <cole o git diff dev...HEAD>
BRANCH: <cole a saída de `git branch --show-current`>
REPO: <creed-backend | creed-frontend | creed-infrastructure>
MAINTAINERS: time `creed-educa-ai/maintainers` (ver creed-ai-context/equipe.md)

FASE 1 — pré-voo. Confira e diga o resultado de cada item, marcando BLOQUEIO ou aviso:
1. Nome da branch em `<slug>/<id-clickup>-<contexto>`, minúsculas, sem acento, com ID
   numérico. Slugs: feat fix refactor perf test docs style chore ci build hotfix release.
2. A branch saiu de `dev` (eu confirmo se você pedir) e não é `dev` nem `main`.
3. O ID da branch é o mesmo da tarefa.
4. Cada critério de aceite da spec aponta para arquivo:linha do diff. Critério não
   atendido é BLOQUEIO.
5. Review completa: corretude, camadas, contratos, dados, segurança, testes,
   legibilidade. "Mudanças necessárias" é BLOQUEIO. Classifique o tier — Sensível
   (migration, autenticação, contrato de API, agregação, infra) exige outra passada com
   modelo pesado e segunda leitura humana antes do PR.
6. Estrutura e nomes: domínio em português sem acento, técnico em inglês, model
   singular, pasta plural, feature = nome do domínio.
7. Nível do código: um colega entende em uma passada. Abstração para um caso só,
   metaprogramação, genérico profundo, `any`/`except: pass` = aviso com o motivo.
8. Sem credencial, `.env` ou dado real no diff. BLOQUEIO.
9. Premissas usadas estão registradas e marcadas.

FASE 2 — apresente:

## Pré-voo do PR — <ID> <título>
Repo · Branch → dev · Tier
### Bloqueios          (ou "nenhum")
### Considerações
### Aderência à spec   (critério → arquivo:linha, ou NÃO atendido)
### O que eu devo executar, nesta ordem
### Descrição do PR    (corpo inteiro, no formato do pr-template)

FASE 3 — só depois de eu responder "aprovado", me dê os comandos exatos: checkout -b,
add dos arquivos um a um (nunca `git add .`), commit em Conventional Commits, push, e
`gh pr create --base dev --reviewer creed-educa-ai/maintainers`. Havendo bloqueio na fase 1, não me dê
comando nenhum: diga o que corrigir.
